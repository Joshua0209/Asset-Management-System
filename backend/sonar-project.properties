# SonarQube — backend module.
# Server URL + auth token come from CI secrets (SONAR_HOST_URL + SONAR_TOKEN).

sonar.projectKey=Joshua0209_Asset-Management-System-backend
sonar.projectName=Asset Management System — Backend
sonar.projectVersion=0.1.0

sonar.sources=app
sonar.tests=tests
sonar.test.inclusions=tests/**/*.py

sonar.python.version=3.11
sonar.python.coverage.reportPaths=coverage.xml

sonar.sourceEncoding=UTF-8
sonar.exclusions=**/.venv/**,**/__pycache__/**,**/alembic/versions/**
