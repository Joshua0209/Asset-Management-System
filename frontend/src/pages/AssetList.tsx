import React from 'react';
import { Typography } from 'antd';

const AssetList: React.FC = () => {
  return (
    <div>
      <Typography.Title level={2}>Asset List</Typography.Title>
      <p>View and manage all organization assets.</p>
    </div>
  );
};

export default AssetList;
