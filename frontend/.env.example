# API base URL — works with Vite proxy in dev, Nginx reverse proxy in prod
VITE_API_BASE_URL=/api/v1

# Set to "true" to enable MSW mock API (FE-only development without backend)
VITE_ENABLE_MOCKS=false
