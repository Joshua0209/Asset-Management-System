# SonarQube — frontend module.
# Server URL + auth token come from CI secrets (SONAR_HOST_URL + SONAR_TOKEN).

sonar.projectKey=Joshua0209_Asset-Management-System-frontend
sonar.organization=joshua0209
sonar.projectName=Asset Management System — Frontend
sonar.projectVersion=0.1.0

sonar.sources=src

sonar.typescript.tsconfigPath=tsconfig.json

sonar.sourceEncoding=UTF-8
sonar.exclusions=**/node_modules/**,**/dist/**,**/build/**,package-lock.json
